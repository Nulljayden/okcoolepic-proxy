/**
 * SoundManager 2: JavaScript Sound for the Web
 * This is the main file for SoundManager 2, a JavaScript library for playing sounds on the web.
 * It is copyrighted by Scott Schiller and provided under the BSD License.
 *
 * @version V2.97a.20110424
 */
(function(Y) { // Y is a parameter representing the global object (window)

  // Constructor function for SoundManager object
  function M(M, X) {
    // M is the first argument, X is the second argument passed to the constructor
    // ... (rest of the code)
  }

  // Create the SoundManager object and assign it to the global object (window)
  window.SoundManager = M;

  // Create a new instance of the SoundManager object and assign it to the global object (window)
  window.soundManager = new M();

})(window);
