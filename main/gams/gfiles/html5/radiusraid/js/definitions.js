/*==============================================================================
Definitions
==============================================================================*/
const definitions = {
  /*------------------------------------------------------------------------------
  Audio
  ------------------------------------------------------------------------------*/
  audio: {
    shoot: {
      count: 10,
      params: [
        [2, 0.2, 0.1753, 0.64, -0.5261, ,, -0.564, ,, ,, 0.5522, -0.564, ,, 1, ,, , 0.25]
      ]
    },
    shootAlt: {
      count: 10,
      params: [
        [0, 0.16, 0.18, 0.18, 0.47, 0.0084, -0.26, ,, ,, 0.74, -1, -0.76, , 1, ,, , 0.15]
      ]
    },
    // ... rest of the audio definitions
  },

  /*------------------------------------------------------------------------------
  Enemies
  ------------------------------------------------------------------------------*/
  enemies: [
    { // Enemy 0 - horizontal / vertical
      value: 5,
      speed: 1.5,
      life: 1,
      radius: 15,
      hue: 180,
      lockBounds: 1,
      setup() {
        // ... setup code
      },
      behavior() {
        // ... behavior code
      }
    },
    // ... rest of the enemy definitions
  ],

  /*------------------------------------------------------------------------------
  Levels
  ------------------------------------------------------------------------------*/
  levels: [],
  generateLevels() {
    const base = 25;
    for (let i = 0; i < definitions.enemies.length; i++) {
      const distribution = [];
      for (let di = 0; di < i + 1; di++) {
        const value = (di === i) ? Math.floor((i + 1) * base * 0.75) : (i + 1) * base;
        distribution.push(value);
      }
      definitions.levels.push({
        killsToLevel: 10 + (i + 1) * 7,
        distribution
      });
    }
  },

  /*------------------------------------------------------------------------------
  Powerups
  ------------------------------------------------------------------------------*/
  powerups: [
    { title: 'HEALTH PACK', hue: 0, saturation: 0, lightness: 100 },
    // ... rest of the powerups
  ],

  /*------------------------------------------------
