// @flow

import { decode as decodeFromDecoder } from "./decoder";
import { encode as encodeFromEncoder } from "./encoder";

export {
  decodeFromDecoder as decode,
  encodeFromEncoder as encode,
};

