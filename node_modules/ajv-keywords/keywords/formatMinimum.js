'use strict';

try {
  const formatLimit = require('./_formatLimit');
  module.exports = formatLimit('Minimum');
} catch (err) {
  console.error('Error requiring _formatLimit module:', err);
  process.exit(1);
}
