import { RuleModule } from './index';

export const BUILTIN_RULES: Map<string, RuleModule> = new Map(); // No need to deprecate if it's not being used

export class FileEnumerator {
  constructor(
    public cwd?: string,
    public configArrayFactory?: any,
    public extensions?: any,
    public globInputPaths?: boolean,
    public errorOnUnmatchedPattern?: boolean,
    public ignore?: any
  ) {}

  public isTargetPath(filePath: string, providedConfig?: any): boolean {
    // implementation
  }

  public *iterateFiles(patternOrPatterns: string | string[]): IterableIterator<{config: any, filePath: string, ignored: boolean}> {
    // implementation
  }
}
