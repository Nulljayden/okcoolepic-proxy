// constants.ts

import * as os from 'node:os';
import * as crypto from 'node:crypto';
import * as fs from 'node:fs';

export const errno = os.errno;
export const priority = os.priority;
export const signals = os.constants.Signal;
export const cryptoConstants = crypto.constants;
export const fsConstants = fs.constants;


// node-constants.ts

import * as constants from './constants';

export = constants;


// some-module.ts

import constants from 'node:constants';

// Use the constants directly from the imported module
constants.errno;
constants.signals;
constants.cryptoConstants;
constants.fsConstants;
