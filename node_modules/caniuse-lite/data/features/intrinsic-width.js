// Define an object with properties A, B, C, ..., S, with each property
// being another object containing key-value pairs.
const myObject = {
  A: {
    "2": "J D E F A B rB"
  },
  B: {
    "2": "C K L G M N O",
    "1537": "P Q R S V W X Y Z a b c d e f g T h H"
  },
  // ...
  S: {
    "932": "qC"
  }
};

// Define a variable for the property B, since it is referenced multiple times.
const propB = myObject.B;

// Define a variable for the property S, since it is referenced multiple times.
const propS = myObject.S;

// Add a new property B5 to the object, with a value of 5.
myObject.B5 = 5;

// Add a new property C with a string value.
myObject.C = "Intrinsic & Extrinsic Sizing";

// Export the object as the module value.
module.exports = myObject;
