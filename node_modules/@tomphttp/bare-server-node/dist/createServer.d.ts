import BareServer from './BareServer.js';
import type { ServerConfig } from './BareServer.js';

const createServer = (directory: string, init?: Partial<ServerConfig>): BareServer => {
  return new BareServer(directory, init);
};

export default createServer;
