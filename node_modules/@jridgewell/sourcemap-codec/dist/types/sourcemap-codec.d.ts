export type SourceMapSegment = [number] | [number, number, number, number] | [number, number, number, number, number];
export type SourceMapLine = ReadonlyArray<SourceMapSegment>;
export type SourceMapMappings = ReadonlyArray<SourceMapLine>;

export function decode(mappings: string): SourceMapMappings {
  // implementation here
}

export function encode(decoded: SourceMapMappings): string {
  // implementation here
}

// Overload the encode function to accept both SourceMapMappings and Readonly<SourceMapMappings>
export function encode(decoded: SourceMapMappings): string;
export function encode(decoded: Readonly<SourceMapMappings>): string;
export function encode(decoded: SourceMapMappings | Readonly<SourceMapMappings>): string {
  // implementation here
}
